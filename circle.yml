test:
    override:
        - node ./node_modules/.bin/mocha -R xunit > $CIRCLE_TEST_REPORTS/testResults.xml
        - npm run coverage
        - npm run analysis
general:
    artifacts:
        - "coverage" # Coverage report
        - "report" # Static analysis
